name: Deploy Avalonia WASM to GitHub Pages

on:
  push:
    branches: [ main ] # Triggers automatically when pushing code
  workflow_dispatch:   # <--- ADD THIS LINE (Enables the manual grey button)

permissions:
  contents: read
  pages: write
  id-token: write
