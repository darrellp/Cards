<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:SolitaireUI.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
             x:Class="SolitaireUI.Views.GameSelectView"
             x:DataType="vm:GameSelectViewModel">

    <Grid Background="#2e7d32">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="30">
            <TextBlock Text="Select a Solitaire Game"
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20" />

            <Border Background="White"
                    CornerRadius="8"
                    Padding="30"
                    MinWidth="400">
                <StackPanel Spacing="20">
                    <TextBlock Text="Available Games:"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,10" />

                    <ListBox ItemsSource="{Binding AvailableGames}"
                             SelectedItem="{Binding SelectedGame}"
                             Height="200"
                             Padding="5">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                          FontSize="16"
                                          Padding="8,5" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button Content="Start Game"
                            Command="{Binding StartGameCommand}"
                            HorizontalAlignment="Center"
                            Padding="30,10"
                            FontSize="18"
                            FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>